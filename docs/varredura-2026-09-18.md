# Varredura curta — 2026-09-18

Alvo diff de HEAD~2..HEAD na branch auto/acervo do repo C:/dev/labirinto — o acervo de tokens: client/src/lib/tokenLibrary.ts, client/src/stores/tokenLibraryStore.ts, client/src/components/TokenLibraryPanel.tsx, client/src/components/TokenImageControls.tsx, client/src/components/SelectionControls.tsx e os handlers do acervo em client/src/App.tsx (handleSaveTokenToLibrary, handlePlaceFromLibrary, handleDeleteFromLibrary, criarToken, pararDeOuvir); lentes que RODARAM: correcao, dados, testes, falso-verde; 15 agentes (teto declarado 32).

## Cobertura e limites deste run

- lente seguranca AUSENTE neste run (modo curta): auth, entrada de usuario e endpoint NAO foram olhados. Para incluir: modo tudo, ou seguranca: true nos args.
- banca de 1 refutador por achado (modo curta): neste run CONFIRMADO se apoia em UMA voz ancorada, nao em duas, e uma refutacao ancorada ja descarta. Peca de gauntlet nascida daqui carrega essa marca; para o regime de duas vozes, rode com refutadoresPorAchado: 2 fora do modo curta.
- 10 achado(s) sairam SEM_REFUTACAO por cap/teto: ver a secao "Fora da refutacao".
- correcao: Não avaliei: cobertura de teste (não existe client/src/lib/tokenLibrary.test.ts; e2e task-jornada-acervo-de-tokens.spec.ts não foi lido nem executado), acessibilidade do TokenLibraryPanel, performance (exists() serial por item em listarAcervo, fonteDaFoto chamado 2x por render), escopo/permissao do plugin fs do Tauri para appDataDir/tokens (não localizei src-tauri neste caminho), e o comportamento real do truque onFocus/select + onMouseUp preventDefault em SelectionControls (precisaria de navegador para confirmar/descartar).
- dados: Nao cobri: concorrencia (duas gravacoes do acervo ao mesmo tempo - `salvarNoAcervo` faz read-modify-write sem trava, e `recarregar()` sem cancelamento), performance (um `exists()` por item a cada `listarAcervo`, e `listarAcervo` chamado 2x por salvar), acessibilidade do painel (botao de foto/nome, foco depois de apagar), seguranca (bytes nao validados como imagem antes de gravar, vindos de `imageData` de mapa de terceiro), e cobertura de teste (nao existe `tokenLibrary.test.ts`; `rg --files client/src/lib \| rg -i tokenlibrary` devolve so o modulo). Tambem nao segui o bug pre-existente de `imageImport.ts:83` (`sourcePath.split('.').pop()` quebra em caminho sem extensao no arquivo mas com ponto na pasta) porque ele vive fora do alvo e nao e alcancavel pelos caminhos do acervo.
- testes: Nao rodei a suite Playwright (precisa de vite servindo + browsers; a lente e cobertura, nao execucao da jornada) — entao a jornada do acervo foi lida, nao vista passar. Nao avaliei corretude da logica do acervo, acessibilidade do painel, performance de listarAcervo (exists() por item, em serie) nem o desenho do portao/particao alem de conferir que a jornada esta na lista do criterio e que o hash do selo bate com o disco. O lado Rust e desktop/src-tauri ficaram fora. Verifiquei que pararDeOuvir (App.tsx:64-84) NAO e achado de cobertura: com StrictMode ligado (main.tsx:15) o ramo de limpeza roda em todo mount, e a jornada 3 vigia pageerror — descartado antes de reportar.
- falso-verde: Nao julguei a logica do acervo em si (corretude de salvarNoAcervo/trazerDoAcervo, UX do painel, CSS, acessibilidade) — outra lente. Nao rodei `--prova` inteiro (cargo clippy, cargo test, transporte-vivo, jornada-fluidez, estilo-minimapa), entao o verde desses passos continua sendo alegacao de terceiro para mim. Nao auditei os outros 59 specs de client/e2e que ficam fora de todo passo do PLANO (portao.cjs:321-323 admite 5 testes ja vermelhos em task-room-tool e task-room-circle-polygon). Nao medi o lado Rust.

## Tabela

| arquivo:linha | severidade | cenário de falha | status |
|---|---|---|---|
| C:/dev/labirinto/client/src/lib/tokenLibrary.ts:287 | alto | Falha de LEITURA do acervo.json vira "acervo vazio" e o proximo salvar/apagar regrava o indice zerado — perde todo o acervo, sem backup — listarAcervo() engole qualquer erro de IO e devolve {itens: [], aviso: '...'} (tokenLibrary.ts:166-168). apagarDoAcervo faz `const { itens } = await listarAcervo()` (linha 287) e DESCARTA o aviso: com 20 NPCs no acervo, se a leitura de acervo.json falhar nesse instante (arquivo travado por antivirus/sync, 'os error 5' — falha que o proprio motivoEmPortugues:106 prevê), `restantes` = [] e gravarIndice([]) reescreve acervo.json como {"versao":1,"itens":[]}. Os 20 itens somem da lista para sempre e as imagens ficam orfas na pasta. salvarNoAcervo (linha 234) tem o mesmo defeito: grava um indice com UM item só. E o backup .invalido só existe no ramo de JSON ilegivel (linhas 171-173) — no ramo de erro de IO não há copia nenhuma. Variante do mesmo defeito: itensDoIndice descarta item individual invalido (linhas 142-147) e o proximo salvar/apagar persiste a lista truncada, apagando de vez itens que uma versao futura do formato gravou. | CONFIRMADO |
| C:/dev/labirinto/client/src/lib/tokenLibrary.ts:234 | alto | Falha de LEITURA do indice vira "acervo vazio", e a proxima gravacao apaga o acervo inteiro — O mestre tem 30 NPCs no acervo. Uma leitura de `acervo.json` falha por I/O (arquivo travado por antivirus/OneDrive/backup no %APPDATA% - sharing violation os error 32, ou permissao negada): `listarAcervo` entra no catch da linha 166 e devolve `{ itens: [], aviso: '...' }`. `salvarNoAcervo` (linha 234) e `apagarDoAcervo` (linha 287) fazem `const { itens } = await listarAcervo()` e DESCARTAM o `aviso` - para eles a lista vazia e indistinguivel de "acervo novo". Salvar o proximo token grava `{versao:1, itens:[soODoNovo]}` (linha 259) por cima do indice bom; apagar um item grava `itens: []` (linha 289). Os 30 nomes somem para sempre e as imagens ficam orfas na pasta. O backup `.invalido` (linha 45) so e escrito no caminho de JSON ilegivel (linha 172) - neste caminho nao ha copia nenhuma. | CONFIRMADO |
| client/e2e/task-jornada-acervo-de-tokens.spec.ts:193 | alto | A jornada prova a foto do acervo com uma foto que o proprio stub devolve: os bytes gravados nunca entram em assercao nenhuma — salvarNoAcervo grava no acervo uma imagem ERRADA mas decodificavel (ex.: a copia embutida de 256px em vez do arquivo escolhido, ou — mais provavel — trazerDoAcervo copiando itens[0].arquivo em vez de item.arquivo quando ha dois NPCs). A jornada continua VERDE: `convertFileSrc` do stub devolve `entrada.dataUrl` (a foto magenta/verde fixa) para QUALQUER caminho que contenha 'token_', e e essa data URL que o <img> da miniatura e o renderer do canvas desenham. As duas assercoes de pixel (spec:375-378) e a de naturalWidth>0 (spec:342-344) medem o stub, nao o disco. Na tela do mestre real o goblin voltaria com a foto do orc, ou com a foto degradada, e o portao nao pisca. A jornada tambem nunca poe DOIS itens com fotos diferentes no acervo (teste 1 tem 1 item, teste 2 tem 1 item), entao o mapeamento id->arquivo nao tem testemunha alguma. | CONFIRMADO |
| client/src/lib/tokenLibrary.ts:133 | medio | Modulo do acervo (351 linhas, 6 exports) sem UM teste de unidade, num repo com 77 testes em lib/ — acervo.json com 5 NPCs e um item gravado pela metade (sem o campo `arquivo`, ou `tamanho` como string vinda de uma versao futura do formato). Hoje `itensDoIndice` descarta so o item quebrado (linhas 142-147) e os outros 4 continuam abrindo. Se um refactor trocar esse `if (item !== null) itens.push(item)` por um `return null` (o mesmo gesto de 'JSON invalido = acervo vazio' que ja existe 4 linhas acima), o usuario perde os 5 NPCs e a suite inteira continua verde: a e2e so cobre JSON TOTALMENTE ilegivel (task-jornada-acervo-de-tokens.spec.ts:415, '{ isto nao e json'), nunca corrupcao parcial. `itensDoIndice` esta exportada (linha 133) e nao tem nenhum consumidor fora do proprio arquivo — foi exportada para ser testavel e nao foi testada. | CONFIRMADO |
| client/src/components/TokenImageControls.tsx:49 | medio | 'Salvar no acervo' em token SEM foto: o caminho mais comum do usuario novo, sem nenhum teste — Pessoa clica 'Adicionar token', digita 'Goblin' e ja clica em 'Salvar no acervo' antes de escolher imagem — o botao aparece sempre (o proprio comentario em TokenImageControls.tsx:44-48 diz que ele fica visivel de proposito, porque a mensagem 'ensina'). O unico retorno e o toast de reportFileError('guardar o token no acervo', SEM_FOTO_PARA_SALVAR). Nada testa esse toast: se reportFileError passar a engolir Error sem `message`, ou se alguem trocar o throw da linha 232 por um `return` silencioso, o clique passa a nao fazer NADA e a suite fica verde — o usuario clica, nada acontece, e ele conclui que o acervo nao funciona. | CONFIRMADO |
| C:/dev/labirinto/client/src/App.tsx:943 | medio | Colocar do acervo gera DUAS entradas de desfazer para um clique, e a segunda chega atrasada limpando o refazer — handlePlaceFromLibrary chama criarToken (→ addToken, mapStore.ts:1142, withHistory) e depois setTokenImage (mapStore.ts:1155, withHistory). Clique em 'Colocar Goblin no mapa' → Ctrl+Z: o primeiro desfazer só tira a FOTO e deixa um 'Goblin' sem imagem no mapa; só o segundo remove a peça. Pior: se a pessoa apertar Ctrl+Z enquanto a copia do arquivo ainda está em voo (importTokenImage + buildTokenSharedPhoto, ~centenas de ms), o setTokenImage tardio roda sobre um mapa que já não tem esse tokenId — mapFactory.setTokenImage:548-553 faz map.tokens.map e não acha nada, virando no-op SILENCIOSO que mesmo assim empilha em `past` e zera `future` (mapStore.ts:845-852), matando o Refazer: o token desfeito não volta mais, e um Ctrl+Z extra não faz nada visivel. O proprio mapFactory.ts:540-546 documenta que image+imageData andam juntas justamente para não dar 'duas entradas de desfazer para um gesto só'. | CONFIRMADO |
| C:/dev/labirinto/client/src/lib/tokenLibrary.ts:252 | medio | `token.name.trim()` sem guarda de tipo, e a imagem ja foi gravada quando ele estoura — Token vindo de mapa legado / map.json editado a mao / `addToken` cru de spec e2e sem o campo `name` - `mapFile.ts:114` normaliza SO `image` (`{ ...t, image: t.image ?? null }`), `name` passa cru. O mestre seleciona esse token e clica "Salvar no acervo": as linhas 240-247 ja gravaram `token_<id>.webp` no disco, e a linha 252 estoura `TypeError: Cannot read properties of undefined (reading 'trim')`. Resultado: (a) toast em ingles tecnico - exatamente o que o docblock das linhas 30-33 promete nunca mostrar; (b) o arquivo de imagem fica orfao na pasta do acervo para sempre, porque `apagarDoAcervo` so alcanca arquivo que tem item no indice. O mesmo estouro derruba qualquer falha posterior a gravacao da imagem (ex.: `gravarIndice` falhando por disco cheio na linha 271). | CONFIRMADO |
| C:/dev/labirinto/client/src/lib/tokenLibrary.ts:56 | medio | O indice guarda caminho ABSOLUTO, entao copiar/migrar a pasta do acervo mata todas as fotos — O acervo e global do app e nao do mapa, entao a forma natural de leva-lo para outro PC (ou para outro usuario Windows, ou depois de reinstalar com perfil novo) e copiar `%APPDATA%\com.labirinto.app\tokens` inteira - indice + imagens. Depois da copia, cada `arquivo` do `acervo.json` ainda aponta para `C:\Users\<usuario-antigo>\AppData\Roaming\com.labirinto.app\tokens\token_x.webp`. `listarAcervo` faz `exists(item.arquivo)` (linha 180) no caminho antigo, marca `imagemNoDisco: false` em TODOS os itens; o painel mostra a estante inteira sem miniatura e cada "colocar no mapa" cai em `trazerDoAcervo` linha 340 devolvendo `{null, null}` -> toast IMAGEM_SUMIU_DO_ACERVO, apesar de os arquivos estarem ali do lado do proprio acervo.json. | CONFIRMADO |
| client/src/lib/tokenLibrary.ts:241 | baixo | O ramo da foto EMBUTIDA (token que veio do jogador) nunca roda em teste nenhum — O jogador escolhe a foto do proprio token e o mestre recebe `{ image: null, imageData: 'data:image/...' }` (playerConnection.ts:532). O mestre seleciona esse token e clica 'Salvar no acervo': `salvarNoAcervo` cai no ramo `isTokenPhotoData(foto)` -> `fotoEmBytes` + `writeFile` (linhas 241-244), caminho COMPLETAMENTE diferente do `importTokenImage`. A jornada e2e so exercita o outro ramo (ela sempre passa por 'Escolher imagem...', spec:304). Se `fotoEmBytes` errar o fatiamento do mime (linha 203 usa indexOf(';') sem checar -1) ou gravar bytes truncados, o item entra no acervo com um arquivo invalido, a miniatura fica quebrada e 'Colocar no mapa' estoura — e nada na suite acusa. | CONFIRMADO |
| client/e2e/task-jornada-acervo-de-tokens.spec.ts:398 | baixo | Apagar do acervo: a varredura que remove os ARQUIVOS nao tem asserção nenhuma — Cada item do acervo deixa DOIS arquivos na pasta (`token_<id>_original.<ext>` sempre, mais `token_<id>.webp` quando houve reamostragem — imageImport.ts:84 e :106-107). `apagarDoAcervo` varre a pasta por prefixo para remover os dois (tokenLibrary.ts:292-300), e o `catch` da linha 297 engole qualquer falha de proposito. A jornada 2 so confere que o item sumiu da LISTA e do indice. Se a varredura quebrar (readDir devolvendo `name` com subcaminho, prefixo mudando, `remove` recusando), o mestre que salva e apaga 100 NPCs acumula 200 imagens orfas no appdata para sempre — e a suite fica verde, porque o erro esta dentro de um catch mudo. | CONFIRMADO |

## Legenda dos status

CONFIRMADO = sobreviveu ao UNICO cetico ancorado deste run (banca de 1 refutador — veja Cobertura e limites).
PLAUSIVEL = ceticos divergiram, ou faltou voz ancorada para o veredito conclusivo deste run (minimo em vigor aqui: 1 voz(es) ancorada(s) para confirmar e 1 para descartar).
NAO_ANCORADO = nenhum cetico conseguiu ancorar nada: o achado nao foi julgado, precisa de ancora a mao.
SEM_REFUTACAO = cap ou teto de recurso: a refutacao nao rodou (motivo na linha).

## Descartados na refutacao

(nenhum)

## Fora da refutacao

- client/e2e/task-jornada-acervo-de-tokens.spec.ts:421 [medio] A jornada do acervo.json ilegivel cobra o aviso e nunca confere a copia de seguranca que o aviso promete (lente testes)
- client/src/lib/tokenLibrary.ts:340 [medio] 'A imagem sumiu do disco' tem tres comportamentos ligados e zero teste (lente testes)
- client/e2e/task-jornada-acervo-de-tokens.spec.ts:193 [medio] O stub de convertFileSrc devolve a MESMA foto para qualquer caminho com 'token_': a medida de pixel nao distingue um item do outro (lente testes)
- scripts/portao.cjs:1861 [medio] O passo que roda a jornada do acervo e `prova: true`: a volta comum do portao nao o alcanca, e `--listar` nao o cita em fora_da_volta (lente falso-verde)
- scripts/portao.cjs:467 [medio] O piso de testes do passo `unidade` voltou a ter folga (2299 declarado contra 2326 medidos) e g24 compara so a CONTAGEM de arquivos, nao a lista (lente falso-verde)
- C:/dev/labirinto/client/src/lib/tokenLibrary.ts:47 [baixo] `versao` e gravada e nunca conferida; a regravacao achata o indice para v1 e descarta campo de versao futura (lente dados)
- client/src/lib/tokenLibrary.ts:255 [baixo] Nome repetido no acervo ('Goblin', 'Goblin (2)') e decisao de produto documentada sem teste — e quebraria a propria jornada (lente testes)
- client/src/lib/tokenLibrary.ts:306 [baixo] renomearNoAcervo exportada sem nenhum chamador e sem nenhum teste (lente testes)
- client/src/stores/tokenLibraryStore.ts:35 [baixo] A unica store que alimenta o painel nao tem teste, e o 'nunca lanca' dela nao e verificado em lugar nenhum (lente testes)
- client/src/lib/tokenLibrary.ts:241 [baixo] 512 linhas novas do acervo com ZERO teste de unidade; o ramo da foto embutida e 4 exports nao sao tocados por jornada nem por teste (lente falso-verde)

## Pecas sugeridas para o gauntlet

- peca = client/src/lib/tokenLibrary.ts (apagarDoAcervo, linha 287) + falha de leitura de acervo.json precisa deixar de ser tratada como "acervo vazio": apagarDoAcervo tem que abortar (ou preservar o indice atual) quando listarAcervo devolve aviso de erro de IO, nunca regravar itens: [] por cima do indice bom.
- peca = client/src/lib/tokenLibrary.ts (salvarNoAcervo, linha 234) + falha de leitura de acervo.json precisa deixar de ser tratada como "acervo vazio": salvarNoAcervo tem que abortar (ou mesclar com o indice existente) quando listarAcervo devolve aviso de erro de IO, nunca sobrescrever o indice com só o item novo.
- peca = client/e2e/task-jornada-acervo-de-tokens.spec.ts (linha 193) + a jornada precisa comparar os BYTES realmente gravados no disco (ou usar fotos distintas por item, com stub que diferencia por caminho real) em vez de medir pixels vindos do stub de convertFileSrc, para provar que trazerDoAcervo devolve o arquivo do item certo quando há dois NPCs no acervo.
- peca = client/src/lib/tokenLibrary.ts (itensDoIndice, linha 133) + criar teste de unidade que grava um índice com um item parcialmente corrompido (sem `arquivo`, ou `tamanho` como string) junto de itens válidos, e provar que só o item quebrado é descartado — os demais continuam no acervo.
- peca = client/src/components/TokenImageControls.tsx (linha 49) + criar teste que clica em "Salvar no acervo" num token sem foto e afirma que o toast SEM_FOTO_PARA_SALVAR aparece, travando qualquer regressão que torne reportFileError silencioso.
- peca = client/src/App.tsx (handlePlaceFromLibrary, linha 943) + colocar um token do acervo precisa gerar UMA entrada de desfazer (ou o Ctrl+Z durante a cópia em voo não pode perder o token do histórico de refazer) — hoje addToken e setTokenImage empilham duas entradas separadas em withHistory.
- peca = client/src/lib/tokenLibrary.ts (salvarNoAcervo, linha 252) + validar/normalizar `token.name` antes de chamar `.trim()`, para que um token sem `name` (mapa legado, map.json editado a mão) não estoure depois de já ter gravado a imagem no disco — e não deixe arquivo órfão.
- peca = client/src/lib/tokenLibrary.ts (linha 56) + o índice do acervo precisa guardar caminho relativo (ou resolver dinamicamente contra o diretório de tokens atual) em vez de caminho absoluto, para sobreviver à cópia/migração da pasta `tokens` para outro usuário ou máquina.
- peca = client/src/lib/tokenLibrary.ts (salvarNoAcervo, ramo de foto embutida, linha 241) + criar teste de unidade/e2e que exercita o caminho `isTokenPhotoData` (token vindo do jogador, com `imageData` em vez de `image`), cobrindo o fatiamento de mime em `fotoEmBytes` e a gravação dos bytes.
- peca = client/e2e/task-jornada-acervo-de-tokens.spec.ts (linha 398) + a jornada de apagar do acervo precisa afirmar que os arquivos `token_<id>_original.*` e `token_<id>.webp` somem de fato do disco, não só que o item some da lista/índice.
