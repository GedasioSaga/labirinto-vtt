# Varredura curta — 2026-09-14

Alvo: Mudanças não commitadas (git diff HEAD) em C:\dev\labirinto: túnel Cloudflare Quick Tunnel no Rust (desktop/src-tauri/src/net/tunnel.rs novo, server.rs, commands.rs, src/lib.rs, tests/net_server.rs, Cargo.toml), lado TS (client/src/net/hostBridge.ts, client/src/components/RoomPanel.tsx, client/src/components/RailTabs.tsx novo, client/src/App.tsx, client/src/main.css) e correção de resize do canvas (client/src/pixi/PixiCanvas.tsx, client/src/player/PlayerView.tsx, client/e2e/task-canvas-resize.spec.ts novo). Contexto: o servidor axum da sala fica exposto à internet via cloudflared enquanto o link público está ativo; jogador entra com código de 6 caracteres. Só leitura; o usuário está com o app aberto: não matar labirinto.exe nem rodar o app. Lentes que RODARAM: correcao, dados, testes, falso-verde, seguranca; 14 agentes (teto declarado 32).

## Cobertura e limites deste run

- seguranca: true no modo curta: a lente de seguranca FOI ACRESCENTADA — a curta vira 5 lentes, com triagem por batedor antes do auditor. A banca continua de 1 refutador, entao achado de seguranca daqui sai apoiado em UMA voz ancorada; para duas vozes, rode modo bugs com seguranca: true.
- banca de 1 refutador por achado (modo curta): neste run CONFIRMADO se apoia em UMA voz ancorada, nao em duas, e uma refutacao ancorada ja descarta. Peca de gauntlet nascida daqui carrega essa marca; para o regime de duas vozes, rode com refutadoresPorAchado: 2 fora do modo curta.
- correcao: Não li todo o diff de App.tsx (524 linhas; olhei só a parte do túnel e do hostBridge), nem main.css, os testes (RoomPanel.test.tsx, hostBridge.test.ts, tests/net_server.rs) e o e2e task-canvas-resize.spec.ts. Não rodei cargo test nem vitest, porque o app está aberto e a regra era só leitura. Não conferi o comportamento do redirect de /player em dev (DEV_PLAYER_URL) com wait_ready.
- dados: Não conferi se o cloudflared repassa o Host público original para 127.0.0.1 (se ele trocar o Host, origin_allowed recusa todo WS vindo do túnel). Isso só se confirma rodando o túnel, e o app não podia ser executado. Também ficaram fora: concorrência entre start, stop e o vigia do túnel, drible do limitador trocando de IPv6 (é da lente de segurança) e o restante do App.tsx, que não tem a ver com túnel nem abas.
- testes: Não rodei cargo test, vitest nem playwright (o app estava aberto e a revisão era só leitura), então não confirmei que as suítes passam. Não li App.tsx nem main.css além do grep: não há teste de App, e a ligação de RailTabs/onTunnelChange no App não foi avaliada. Ficaram de fora ensure_cloudflared/download_to/spawn_tunnel/wait_ready (I/O real, sem teste, mas sem um cenário de regressão concreto além do que já está no achado 1) e a posição exata do PROBE do e2e de resize em relação ao rail lateral.
- falso-verde: Não rodei o Playwright (task-canvas-resize.spec.ts), para não subir dev server/navegador com o app do usuário aberto. Por isso não confirmei que o e2e passa nem que a sonda de pixel falha sem o conserto. Não achei no client/src a definição de --lb-color-ink, então não sei se o fundo da página fica dentro da tolerância de 4 em torno de #202020. Se ficar, o expectOutsideShadeAtProbe passaria sem o conserto e o assert não morderia. Não rodei cargo clippy nem tsc. Não fiz teste de mutação real, porque é read-only; os cenários de 'continua verde' vêm da ausência de chamadas nos testes, confirmada por rg. Confiar em Cf-Connecting-Ip e Origin do túnel é assunto da lente de segurança, não desta.
- seguranca: Pontos lidos e considerados seguros: origin_allowed exige loopback, Host igual ao túnel e Origin https do túnel; cf-connecting-ip só é confiado quando a conexão vem do loopback com Host do túnel; parse_quick_tunnel_url é restrito a [a-z0-9-] e ao sufixo exato; o download do cloudflared tem URL, tamanho e SHA-256 fixos; o spawn usa args sem shell; o QR é exibido como <img> com data URL encodeURIComponent e a URL como texto React (sem sink de HTML); is_safe_asset_path bloqueia traversal. Ficaram fora: semgrep, gitleaks e trivy (não rodados); leitura completa de handle_socket e de como o hostBridge repassa net:message dos jogadores ao estado do mestre; tests/net_server.rs; App.tsx, RailTabs.tsx, PixiCanvas.tsx e PlayerView.tsx (a mudança de resize não tem superfície de entrada nova aparente, mas não foi lida linha a linha); TOCTOU do binário cloudflared em app_local_data_dir, que exigiria atacante local com o mesmo usuário (fora do modelo). O comportamento real da borda da Cloudflare (reescrita de Host, limites de requisições do Quick Tunnel) não foi verificado ao vivo, porque o app não podia ser executado.

## Tabela

| arquivo:linha | severidade | cenário de falha | status |
|---|---|---|---|
| C:\dev\labirinto\desktop\src-tauri\src\net\commands.rs:243 | alto | net_start_tunnel pega a geração só depois de conseguir o lock, então um stop feito enquanto o start esperava na fila se perde e o túnel sobe mesmo assim — 1) O mestre clica 'Tornar pública' (start A). O Rust fica preso em spawn_tunnel, que espera a URL por até 30 s e não confere cancelamento. 2) Clica 'Encerrar link público'. O front chama resetTunnel e o Rust soma 1 na geração. 3) Clica 'Tornar pública' de novo (start B): pendingTunnel voltou a null, então o front deixa. No Rust, B fica parado em tunnel_start.lock(). 4) Clica 'Encerrar' outra vez. O front zera activeTunnelAttempt e o Rust soma 1 na geração enquanto B ainda espera. 5) A termina, vê que foi cancelado e solta o lock. B pega o lock, chama kill_tunnel_now (soma 1 de novo) e só então lê a geração na linha 244. O stop do passo 4 some. B sobe o cloudflared, grava set_tunnel_host e emite 'ready'. O front ignora esse 'ready' em hostBridge.ts onTunnel, porque starting e wasReady são false, e também descarta a resposta do invoke (attempt !== activeTunnelAttempt). Resultado: a tela mostra o botão 'Tornar pública' (estado parado), mas a sala segue aberta na internet em https://<nome>.trycloudflare.com e aceita join de quem tiver o código. A mesma perda acontece com um stop dentro de state.room.lock().await (linha 235), que fica entre o lock e a leitura da geração. | CONFIRMADO |
| C:\dev\labirinto\desktop\src-tauri\src\net\commands.rs:58 | medio | O que tira o túnel da sala (kill_tunnel_now, watch_tunnel, cancelamento por geração, net_stop_room) não tem teste; o verde da suite Rust não diz nada sobre fechar a exposição pública — A regra que importa aqui é: fechou a sala ou o link, o servidor deixa de aceitar o nome trycloudflare. Quem cumpre isso é o código de ligação em commands.rs: linha 68 'active.room.clear_tunnel_host()', linha 338 no watcher, e as checagens de geração nas linhas 247, 290 e 308. Nenhum teste chega nesse código. Exemplo: se alguém apagar a linha 68, ou quebrar o 'cancelled()' da linha 290 (stop durante o download deixa set_tunnel_host ativo e o cloudflared vivo), os 25 testes Rust e os 49 do vitest continuam passando. O teste integrado 'upgrade_do_tunel_so_e_aceito_com_tunnel_host_definido' chama room.clear_tunnel_host() direto (tests/net_server.rs:277), sem passar pelos comandos. Do lado TS, hostBridge.test.ts troca o invoke por mock e parte do princípio de que o Rust limpa o estado. Resultado: o portão fica verde enquanto a sala continua exposta à internet depois do stop. | CONFIRMADO |
| C:\dev\labirinto\desktop\src-tauri\src\net\server.rs:345 | medio | Com o túnel ligado, os limites por IP (bloqueio por código errado e vagas pendentes) contam o IPv6 inteiro, então trocar de endereço dentro do mesmo /64 os ignora — Um jogador recebe o link trycloudflare, que costuma ser postado em Discord ou grupo, e o repassa. Quem tem o link e acesso IPv6 abre WebSockets em /ws, cada um saindo de um endereço diferente do mesmo /64, com um join por conexão e um código chutado. O cf-connecting-ip muda a cada conexão. Assim o BadCodeLimiter (5 erros e bloqueio de 60 s por IP) nunca dispara, e o teto de 4 conexões pendentes por IP também não. Sobra só o teto global de 32 conexões pendentes. Consequências: (a) chute do código de 6 caracteres (31^6, cerca de 887 milhões de combinações) só com o freio da latência, sem bloqueio; (b) mais concreto, as 32 vagas pendentes ficam ocupadas o tempo todo (cada uma dura até 5 s de JOIN_TIMEOUT) e o jogador legítimo recebe 503 ao tentar entrar. Antes do túnel isso só era possível de dentro da LAN; agora vale para qualquer pessoa na internet que tenha o link. | CONFIRMADO |
| C:\dev\labirinto\desktop\src-tauri\src\net\commands.rs:39 | baixo | Ciclo de vida do túnel no NetState não tem nenhum teste: geração/cancelamento, kill_tunnel_now limpando tunnel_host, close_tunnel_quietly e o watcher — Suponha que alguém refatore e kill_tunnel_now deixe de chamar active.room.clear_tunnel_host(), ou que o check de cancelled() antes de set_tunnel_host (commands.rs, bloco após spawn_tunnel) saia. O mestre clica em 'Encerrar link público' no meio do wait_ready. O cloudflared morre, mas a sala continua aceitando upgrade com o Host do túnel e confiando no Cf-Connecting-Ip vindo do loopback. Os testes seguem verdes: o único teste de túnel (tests/net_server.rs) chama room.set_tunnel_host/clear_tunnel_host direto na Room, sem passar pelo NetState. | CONFIRMADO |
| C:\dev\labirinto\desktop\src-tauri\tests\net_server.rs | baixo | Limite de pendentes por IP (MAX_PENDING_PER_IP) nunca é exercitado pelo túnel: regressão para peer.ip() passaria verde — Suponha que PendingSlot::acquire volte a receber peer.ip() em vez do IP efetivo (server.rs, ws_handler). Aí todo visitante vindo da internet conta como 127.0.0.1. Numa mesa com 5 jogadores abrindo o link público ao mesmo tempo, o 5º recebe 503 (MAX_PENDING_PER_IP = 4). O teste de 429 não pega isso, porque cobre só o is_blocked/record_bad_code. O teste de 503 só conecta pela LAN. | CONFIRMADO |
| C:\dev\labirinto\client\src\net\hostBridge.ts | baixo | Queda do link público depois de pronto (evento error com túnel ready, closed com reason exited) não tem teste: o toast pode sumir sem ninguém ver — O túnel está ready e o cloudflared cai no meio do jogo. O Rust (watch_tunnel) emite {state:'closed',reason:'exited'}. Em onTunnel, o toast 'O link público caiu: o cloudflared encerrou' depende de 'wasReady' ter sido lido antes de setTunnel(TUNNEL_IDLE). Se a ordem mudar numa refatoração, ou se o ramo 'error' com '!starting' deixar de chamar reportError, o mestre perde o único aviso de que os jogadores remotos caíram. Nenhum teste falha: o único teste de closed usa reason 'stopped' e só confere o estado idle. | CONFIRMADO |
| C:\dev\labirinto\client\src\player\PlayerView.tsx:362 | baixo | O conserto de resize no PlayerView (ResizeObserver) não tem teste; o spec novo só exercita o editor — task-canvas-resize.spec.ts entra pelo enterEditor e mede o canvas dentro de '.lb-editor'. O ResizeObserver incluído no PlayerView.tsx (linhas 362-365 e o disconnect na 377) não passa por nenhum teste. Exemplo: se o observe(el) sumir ou for para antes do app.init, a tela do jogador volta a ficar com faixa escura ao maximizar, e o portão (vitest + e2e) continua verde. Detalhe no próprio spec: o teste 1 usa setViewportSize, que dispara 'resize' da janela e já seria tratado pelo ResizePlugin sem o observer. Quem cobre o ResizeObserver no editor é só o teste 2. | CONFIRMADO |

## Legenda dos status

CONFIRMADO = sobreviveu ao UNICO cetico ancorado deste run (banca de 1 refutador — veja Cobertura e limites).
PLAUSIVEL = ceticos divergiram, ou faltou voz ancorada para o veredito conclusivo deste run (minimo em vigor aqui: 1 voz(es) ancorada(s) para confirmar e 1 para descartar).
NAO_ANCORADO = nenhum cetico conseguiu ancorar nada: o achado nao foi julgado, precisa de ancora a mao.
SEM_REFUTACAO = cap ou teto de recurso: a refutacao nao rodou (motivo na linha).

## Descartados na refutação

(nenhum)

## Fora da refutação

(nenhum)

## Peças sugeridas para o gauntlet

- peça = C:\dev\labirinto\desktop\src-tauri\src\net\commands.rs (net_start_tunnel) + precisa passar a ser verdade: um stop chamado enquanto um start concorrente está preso esperando o lock/URL do túnel nunca é perdido — a geração é capturada antes de qualquer espera bloqueante, e todo caminho de sucesso confere cancelamento pela geração antes de aplicar set_tunnel_host/emitir 'ready'.
- peça = C:\dev\labirinto\desktop\src-tauri\src\net\commands.rs (kill_tunnel_now, watch_tunnel, net_stop_room) + precisa passar a ser verdade: existe teste que passa pelos comandos do NetState (não direto na Room) provando que fechar a sala ou o link público sempre deixa clear_tunnel_host() aplicado e o cloudflared encerrado.
- peça = C:\dev\labirinto\desktop\src-tauri\src\net\server.rs (PendingSlot::acquire, BadCodeLimiter) + precisa passar a ser verdade: o limite de tentativas e de conexões pendentes por IP é resistente a troca de endereço dentro do mesmo prefixo /64 IPv6 quando a conexão vem via túnel público.
- peça = C:\dev\labirinto\desktop\src-tauri\src\net\commands.rs (NetState, ciclo de vida do túnel) + precisa passar a ser verdade: existe cobertura de teste (não só na Room) para geração/cancelamento, limpeza de tunnel_host em kill_tunnel_now, close_tunnel_quietly e o watcher.
- peça = C:\dev\labirinto\desktop\src-tauri\tests\net_server.rs (MAX_PENDING_PER_IP via túnel) + precisa passar a ser verdade: um teste de integração conecta simulando IP efetivo vindo do túnel (não peer.ip() de loopback) e confirma que o 5º pendente do mesmo IP recebe 503.
- peça = C:\dev\labirinto\client\src\net\hostBridge.ts (onTunnel, evento closed/error após ready) + precisa passar a ser verdade: existe teste cobrindo queda do túnel depois de 'ready' (reason 'exited' e ramo 'error' com túnel ativo), garantindo que o toast de aviso sempre dispara.
- peça = C:\dev\labirinto\client\src\player\PlayerView.tsx (ResizeObserver) + precisa passar a ser verdade: existe teste e2e que redimensiona a tela do jogador (não só a do editor) e confirma que o canvas ocupa o container sem faixa escura, exercitando o ResizeObserver e não só o evento 'resize' da janela.
